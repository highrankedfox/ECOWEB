{% extends 'base.html.twig' %}
{% block title %}Espace formateur{% endblock %}
{% block content %}
    <div class="blockcontainer text-center">
        <h1>Espace formateur</h1>
        <br>
        <p>
            Bienvenue dans votre espace formateur.
            Ici, vous pouvez créer de nouvelles formations et retrouver et modifier vos formations déjà en ligne.
        </p>
            <h2>Mes formations</h2> <a class="d-flex align-items-center small btn btn-success custom-btn rounded-pill px-3 mb-2 mb-lg-0" href="#" role="button"><i class="bi bi-plus-circle-fill me-2"></i> nouvelle formation</a>
            {% if app.user.formations|length == 0 %}
                <p class="text-center">Vous n'avez pas encore ajouté de formations, pour commencer cliquez <a href="{{ path('app_formations_add') }}">ici</a>.</p>
            {% else %}
                <div class="row">
                    {% for formation in app.user.formations %}
                        <div class="col-md-4">
                            <div class="card" style="width: 18rem;">
                                <div class="card-body">
                                    <h5 class="card-title">{{ formations.title }}</h5>
                                    <p class="card-text">
                                        {{ formation.description }}
                                    </p>
                                    <a href="">Modifier</a>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            {% endif %}
    </div>
{% endblock %}