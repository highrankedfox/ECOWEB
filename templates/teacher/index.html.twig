{% extends 'base.html.twig' %}
{% block title %}Espace formateur{% endblock %}
{% block content %}
    <div class="blockcontainer text-center">
        <h1>Espace formateur</h1>
        <p>
            Bienvenue dans votre espace formateur. <br>
            Ici, vous pouvez créer de nouvelles formations et retrouver et modifier vos formations déjà en ligne.
            <br>
            <a class="small btn btn-success custom-btn rounded-pill px-3" href="{{path('app_teacher_add')}}" role="button"><i class="bi bi-plus-circle-fill me-2"></i> nouvelle formation</a>
        </p>
        <h2>Mes formations</h2>
        {% if app.user.formations|length == 0 %}
            <p class="text-center">Vous n'avez pas encore ajouté de formation.</p>
        {% else %}
                <div class="row">
                    {% for formation in app.user.formations %}
                    <div class="col-md-4">
                            <div class="card" style="width: 18rem;">
                                <img class="card-img-top" src="{{ vich_uploader_asset(formation, 'imageFile') }}" alt="{{ formation.imageName }}">
                                <div class="card-body">
                                    <h5 class="card-title">{{ formation.title }}</h5>
                                    <p class="card-text">{{ formation.description }}</p>
                                    <a class="small btn btn-success custom-btn rounded-pill px-3" href="" role="button">modifier</a>
                                </div>
                            </div>
                    </div>
                    {% endfor %}
                </div>
        {% endif %}
    </div>
{% endblock %}